# 项目协作约束

本文约定开发者与 AI Agent 在本项目中的协作方式。除非用户明确提出不同要求，均应遵守以下规则。

## 开发前

- 先阅读需求、相关源码、现有测试和 `docs/CHANGELOG.md`，确认实际行为后再修改。
- 开始前检查 Git 工作区状态；已有改动默认属于用户，不覆盖、不丢弃、不顺手整理。
- 明确本次需求范围，只处理完成需求所必需的文件和行为。
- 如果需求存在不影响整体方向的小歧义，采用风险最低、容易回退的合理假设继续；会显著改变产品行为或数据的事项先询问用户。
- 涉及依赖、平台能力、部署规则或其他可能变化的信息时，优先核对当前官方文档。

## 开发中

- 优先复用现有结构、组件和工具，避免无关重构、批量格式化和依赖升级。
- 保持改动小而集中；不得为了“顺便优化”修改需求之外的界面、文案、配置或业务逻辑。
- 不主动修改 `README.md`、`docs/README.zh-CN.md` 或其他对外说明文件；只有用户明确要求时才修改。
- 不主动创建提交、推送代码、发布网站或变更远端配置；只有用户明确授权时才执行。
- 不写入密钥、令牌、账号信息或其他敏感数据；环境差异通过被忽略的本地环境文件或平台配置处理。
- 发现与本次任务无关的问题时，只记录或告知，不擅自修复。
- 涉及删除、覆盖、迁移等难以恢复的操作前，先确认目标和影响范围。

## 验证

- 修改后执行与风险相匹配的测试、类型检查、Lint 或构建检查。
- 优先验证用户实际会走的关键路径，而不只确认命令成功退出。
- 如果某项检查因既有问题或环境限制无法通过，应明确区分“本次改动问题”和“原有问题”，不得隐瞒。
- 修复测试时不得只改断言来掩盖产品缺陷；断言本身过期时，应说明并使其与当前需求一致。

## 完成后

- 确认需求点全部完成，并检查没有遗留调试代码、临时文件或无关修改。
- 更新开发日志前，先检查 Git 工作区中的全部未提交修改，确认本次待提交内容的实际范围。
- 一个提交必须且只能对应 `docs/CHANGELOG.md` 中的一条开发日志；同一批未提交修改已有记录时，应更新该记录，不得为后续需求重复追加记录。
- 开发日志应在提交前维护完成，并使用一句简短内容概括该提交的核心结果。
- 开发日志按功能主题归纳即可，不逐项罗列需求、文案、数值或视觉微调；避免记录冗长过程和无意义细节。
- 最终反馈应简要说明完成内容、验证结果、已知限制以及用户仍需执行的操作。
- 未经用户要求，不在最终反馈中建议额外扩展需求。

## 文件与提交约定

- 新文件应放入与其职责匹配的现有目录；没有充分理由不新增顶层目录。
- 生成文件、缓存、构建产物和本地配置不得提交，必要时更新 `.gitignore`。
- 保持提交主题单一。用户要求提交时，提交信息应简洁描述结果，不使用含糊的“update”或“fix stuff”。
- 不改写用户提交历史，不执行强制推送或破坏性 Git 命令，除非用户明确要求并已确认风险。
